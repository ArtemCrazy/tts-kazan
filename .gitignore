# Исходники шрифта. Репозиторий публичный: выкладывать в него полный набор
# начертаний — это распространение шрифта, а не использование на сайте.
# На сайт уходит только подрезанный actay-wide.woff2, оригиналы лежат
# у владельца в Downloads.
source/fonts/

# Служебные файлы агентов — на прод и в общую историю не идут
.claude/
.renders/

# Мусор среды выполнения
__pycache__/
*.pyc
.DS_Store
Thumbs.db
