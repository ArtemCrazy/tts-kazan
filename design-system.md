# Дизайн-система ТТС Инжиниринг

Источник: **брендбук ТТС 2026** (`.claude/context/media/2026-09-02/1605-document.pdf`, прислан клиентом 02.09.2026),
логотипы оттуда же, фирменная вёрстка сверена с брошюрами ЗССС / БСУ / ВПИ.

Клиент отдельно указал: компания недавно прошла **ребрендинг**, присланные документы имеют приоритет
над всем, что можно увидеть на действующем сайте tts-kazan.ru.

---

## Цвета

Названия — из брендбука, роли — наши.

| Токен | HEX | Имя в брендбуке | Роль |
|---|---|---|---|
| `--c-surface` | `#FFFFFF` | Чистая форма | основной фон |
| `--c-surface-alt` | `#EDEDED` | — | второй фон, разделители |
| `--c-ink` | `#0C2233` | Глубина технологии | текст, тёмные секции |
| `--c-ink-soft` | `#1C3C55` | — | второй тон тёмного |
| `--c-accent` | `#238E30` | Инженерный изумруд | кнопки, акценты, плашки |
| `--c-accent-deep` | `#008251` | — | нижняя точка зелёного градиента |

Градиенты брендбука:

- **Арктический свет** — `#FFFFFF → #EDEDED`, светлые секции
- **Стальной горизонт** — `#0C2233 → #1C3C55`, тёмные секции и первый экран
- **Энергия роста** — `#238E30 → #008251`, кнопки и акцентные плашки

Производные для интерфейса (наши, в брендбуке их нет — помечаю честно):
текст на тёмном `rgba(255,255,255,.72)`, границы на тёмном `rgba(255,255,255,.14)`,
приглушённый текст на светлом `#5A6B7A`.

---

## Типографика

| Шрифт | Начертания | Роль |
|---|---|---|
| **Actay** | Condensed, Regular, **Wide** | заголовки, крупные акценты |
| **Montserrat** | Light → Black | весь остальной текст, интерфейс |

⚠️ **Actay не найден ни в Google Fonts, ни в присланных файлах.** Сейчас в стеке стоит первым,
фактически отрисовывается Montserrat. Как только клиент пришлёт файл — подменяется одной строкой
в `tokens.css`, вёрстку трогать не нужно. **Запросить у клиента.**

Montserrat подключён из Google Fonts. Перед продакшеном — положить файлы шрифта к себе на сервер,
чтобы не тянуть сторонний домен и уложиться в Core Web Vitals.

Шкала (десктоп → мобильный, через `clamp`):

| Токен | Размер | Применение |
|---|---|---|
| `--fs-h1` | 60 → 32 px | заголовок первого экрана |
| `--fs-h2` | 44 → 26 px | заголовки секций |
| `--fs-h3` | 24 → 19 px | заголовки карточек |
| `--fs-lead` | 19 → 16 px | подзаголовок под H1 |
| `--fs-body` | 16 px | основной текст |
| `--fs-small` | 14 px | подписи |
| `--fs-eyebrow` | 12 px | надзаголовок, капс, `letter-spacing .14em` |

Заголовки — **капсом**, это приём брендбука. Фирменная деталь: одна строка заголовка
подсвечивается зелёной плашкой (`--c-accent`), как на развороте «Стандарты коммуникации».

---

## Сетка и ритм

- Контейнер: `max-width 1320px`, боковые поля 40 px (десктоп) → 16 px (мобильный)
- Колонки первого экрана: `1.05fr / 0.95fr`, схлопывается в одну колонку на 1024 px
- Шкала отступов: 4 · 8 · 12 · 16 · 24 · 32 · 48 · 64 · 96 · 128
- Вертикальный ритм секций: 96 px десктоп → 56 px мобильный

Плотность — «инженерная»: воздуха достаточно, но не разрежённо. Брендбук строит композицию
на крупных плотных блоках и жёстких прямых углах.

---

## Форма и глубина

- **Скругления:** 2 px у плашек и кнопок, 4 px у карточек. Язык брендбука — почти острые углы,
  никаких «таблеток».
- **Тени:** только на тёмном фоне и у всплывающих меню — `0 24px 60px rgba(4,14,24,.28)`.
  В плоских секциях глубина показана цветом, а не тенью.
- **Фоновый знак:** шестигранник из логотипа, крупно, с прозрачностью ~4–6 % — приём с обложки брендбука.
- **Фото:** тёмные индустриальные кадры, поверх — затемнение градиентом «Стальной горизонт»,
  чтобы текст читался. Углы прямые, кадр во всю высоту блока.

---

## Точки перестроения

`1440 · 1280 · 1024 · 768 · 390 · 360` — как требует ТЗ (п. 3.3).
Проверять в Chrome, Edge, Firefox, Safari, плюс Android и iOS.

---

## Движение

Характер — «сдержанно и уверенно», без развязных эффектов. Тяжёлое промышленное оборудование
не скачет.

| Токен | Значение |
|---|---|
| `--dur-fast` | 160 ms |
| `--dur-base` | 280 ms |
| `--dur-slow` | 700 ms |
| `--ease` | `cubic-bezier(.22,.61,.36,1)` |

- Появление первого экрана: элементы всплывают снизу на 16 px по очереди, шаг 90 ms.
- Наведение на кнопку: подъём на 1 px + уплотнение цвета, без масштабирования.
- Шапка при прокрутке: из прозрачной становится плотной тёмной с размытием.
- Всё движение отключается при `prefers-reduced-motion` — требование доступности из ТЗ.

---

## Компоненты первого экрана

**Кнопка**
- Варианты: `primary` (зелёный градиент, белый текст), `outline` (прозрачная, светлая рамка).
- Состояния: обычное, наведение, нажатие, фокус (видимая рамка — WCAG), недоступное.
- На мобильном растягивается на всю ширину.

**Шапка**
- Прозрачная поверх первого экрана, при прокрутке — плотная.
- Выпадающие меню «Каталог» (5 направлений + «Весь каталог») и «Сервис» (2 пункта).
- Открываются наведением **и** клавиатурой, закрываются по `Esc` — по ТЗ функции не должны
  зависеть только от мыши.
- На ширине ≤ 1024 px сворачивается в кнопку-бургер со шторкой.

**Верхняя строка**
- Два коротких сообщения, капс, мелкий кегль. На мобильном второе прячется.

**Бейдж-галочка**
- Зелёная галочка + текст. Три штуки в ряд под кнопками, в столбик на мобильном.

---

## Что додумано, а не взято из источника

Помечаю честно, чтобы потом не выдавать догадки за требования:

1. Оттенки интерфейса — приглушённый текст, границы, тексты на тёмном. В брендбуке их нет.
2. Шкала размеров шрифта и отступов. В брендбуке заданы только сами шрифты и палитра.
3. Скругления и тени — считаны с вёрстки брошюр, отдельного правила в брендбуке нет.
4. Тайминги анимации — брендбук про печать, движения в нём нет вовсе.
5. Замена Actay на Montserrat — **вынужденная**, до получения файла шрифта.
