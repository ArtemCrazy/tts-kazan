<?php
/**
 * Главная страница.
 *
 * Блоки главной (первый экран, квиз подбора, категории, ВПИ, бетонные заводы,
 * о компании, сервис, ПКН, проекты, FAQ, форма — п. 6 ТЗ) собираются в Gutenberg
 * из блоков темы, поэтому шаблон только выводит содержимое страницы.
 */

defined( 'ABSPATH' ) || exit;

get_header();

if ( have_posts() ) {
	while ( have_posts() ) {
		the_post();
		the_content();
	}
}

get_footer();
